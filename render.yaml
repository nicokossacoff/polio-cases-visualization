services:
  - type: web
    name: polio-dash-app
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:server
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    plan: free
